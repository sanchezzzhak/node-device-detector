name: Run EsLint

on:
  pull_request:
  push:
    branches: [ master ]

jobs:
  es-lint:
    name: EsLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: yarn
      - name: Run ESLint
          run: eslint . --ext .ts